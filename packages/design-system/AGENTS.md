This package is the shared React design system.

Read `../../docs/agents/design-system.md` before adding or changing components. Treat `package.json` exports as the public interface, keep stories beside components, and verify changes with `pnpm run --filter @boilerplate/design-system typecheck`.
